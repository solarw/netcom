# XStream Examples

Этот каталог содержит примеры использования протокола XStream для различных сценариев.

## 🚀 Quick Start

### Предварительные требования

Убедитесь, что у вас установлен:
- Rust (последняя стабильная версия)
- Cargo

### Запуск примеров

Каждый пример можно запустить отдельно:

```bash
# Базовый пример использования
cargo run --example basic_usage

# Пример механизма принятия решений
cargo run --example inbound_decision
```

## 📋 Available Examples

### 1. Basic Usage (`basic_usage.rs`)

**Описание:** Простейший пример создания двух узлов XStream и обмена данными в стиле RPC.

**Что демонстрирует:**
- Создание узлов XStream
- Установка соединения между узлами
- Открытие потока и обмен данными
- Обработка событий XStream
- Закрытие потоков

**Сценарий использования:**
- Простые RPC взаимодействия
- Обучение основам XStream
- Тестирование базовой функциональности

### 2. Inbound Decision Mechanism (`inbound_decision.rs`)

**Описание:** Продвинутый пример использования механизма принятия решений о входящих соединениях.

**Что демонстрирует:**
- Создание узла с политикой ручного принятия решений
- Фильтрацию входящих соединений по различным критериям
- Отклонение соединений с указанием причины
- Интеграцию с внешней логикой принятия решений

**Критерии принятия решений:**
1. **Лимит одновременных соединений** - ограничение количества активных подключений
2. **Белый список пиров** - динамическое управление разрешенными узлами
3. **Дополнительные проверки** - возможность расширения логики

**Сценарий использования:**
- Защищенные P2P приложения
- Системы с ограниченными ресурсами
- Приложения с требованиями безопасности
- Управление нагрузкой

## 🛠️ Building Examples

### Добавление примеров в Cargo.toml

Убедитесь, что в `Cargo.toml` проекта добавлены необходимые зависимости:

```toml
[[example]]
name = "basic_usage"
path = "examples/basic_usage.rs"

[[example]]
name = "inbound_decision" 
path = "examples/inbound_decision.rs"
```

### Компиляция и запуск

```bash
# Компиляция всех примеров
cargo build --examples

# Запуск конкретного примера
cargo run --example basic_usage
cargo run --example inbound_decision
```

## 🔧 Customization

### Создание собственного примера

1. Создайте новый файл в папке `examples/`
2. Добавьте описание в `[[example]]` секцию `Cargo.toml`
3. Используйте базовый шаблон:

```rust
use xstream::behaviour::XStreamNetworkBehaviour;
use libp2p::{Swarm, identity, PeerId};

#[tokio::main]
async fn main() -> Result<(), Box<dyn std::error::Error>> {
    // Создание узла
    let local_key = identity::Keypair::generate_ed25519();
    let local_peer_id = PeerId::from(local_key.public());
    
    let transport = libp2p::development_transport(local_key).await?;
    let behaviour = XStreamNetworkBehaviour::new();
    
    let mut swarm = SwarmBuilder::with_tokio_executor(transport, behaviour, local_peer_id).build();
    
    // Ваша логика...
    
    Ok(())
}
```

## 🧪 Testing Examples

### Проверка работоспособности

```bash
# Проверка компиляции всех примеров
cargo check --examples

# Запуск с подробным выводом
RUST_LOG=debug cargo run --example basic_usage
```

### Отладка

Для отладки используйте переменные окружения:

```bash
# Подробное логирование
RUST_LOG=debug cargo run --example basic_usage

# Логирование только XStream
RUST_LOG=xstream=debug cargo run --example basic_usage
```

## 📚 Learning Path

Рекомендуемый порядок изучения:

1. **Начинающим:** `basic_usage.rs` - основы работы с XStream
2. **Продвинутым:** `inbound_decision.rs` - управление входящими соединениями

## 🤝 Contributing

Хотите добавить свой пример? Создайте pull request с:

- Четким описанием сценария использования
- Комментариями в коде
- Инструкциями по запуску
- Тестами (если применимо)

## 🔗 Related Documentation

- [Основная документация XStream](../readme.md)
- [API Reference](../src/lib.rs)
- [Тесты](../src/tests/)

---

**Примечание:** Примеры используют memory транспорт для простоты тестирования. В production среде замените на реальные транспорты (TCP, WebRTC, etc).
